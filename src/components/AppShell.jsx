// src/components/AppShell.jsx
import React from "react";

/**
 * Temporary no-op layout to satisfy legacy imports while we migrate pages
 * off AppShell. It just renders children as-is.
 */
export default function AppShell({ children }) {
  return <>{children}</>;
}
